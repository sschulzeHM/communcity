<ul class="nav navbar-nav navbar-right">
  <% unless signed_in?%>
      <li>
      <div class="btn-group">
        <div class="btn-group">
          <button type="button" class="btn navbar-btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
            Anmelden!<span class="caret"></span>
          </button>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "User", new_user_registration_path %></li>
            <li><%= link_to "Organisation", new_organisation_registration_path %></li>
          </ul>
        </div>
        <div class="btn-group">
          <button type="button" class="btn navbar-btn btn-success dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
            Einloggen<span class="caret"></span>
          </button>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "User", new_user_session_path %></li>
            <li><%= link_to "Organisation", new_organisation_session_path %></li>
          </ul>
          </div>
          </div>
      </li>

  <% else %>
      <li class="dropdown">
        <% if user_signed_in?%>
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= concat_username(current_user)%> <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'Mein Profil', user_path(current_user) %></li>
              <li><%= link_to 'Profil bearbeiten',edit_user_registration_path%></li>
              <li class="divider"></li>
              <li></span><%= link_to "Abmelden", destroy_user_session_path, method: :delete %></li>
            </ul>
        <% elsif organisation_signed_in?%>
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= current_organisation.name%> <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'Neues Projekt erstellen', new_project_path%></li>
              <li><%= link_to 'Profil ansehen', organisation_path(current_organisation) %></li>
              <li><%= link_to 'Profil bearbeiten',edit_organisation_registration_path%></li>
              <li class="divider"></li>
              <li><%= link_to "Abmelden", destroy_organisation_session_path, method: :delete %></li>
            </ul>
        <%end%>

  <% end %>
  </li>
</ul>